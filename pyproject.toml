[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I"]   # E: pycodestyle errors, F: pyflakes, I: isort

[tool.pytest.ini_options]
testpaths = ["src/tests"]
